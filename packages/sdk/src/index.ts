/**
 * Perch embed script — entry point.
 *
 * Placeholder during scaffolding. The public surface, the host/frame message
 * contract, and the overlay live here from the next phase onward.
 */

export {};
